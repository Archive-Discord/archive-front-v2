import { NextApiRequest, NextApiResponse } from "next";
import Handler from "@utils/RequestHandler";

export default Handler('createLimiter').get(async (req: NextApiRequest, res: NextApiResponse) => {
  
})

