import { NextApiRequest, NextApiResponse } from "next";
import Handler from "@utils/RequestHandler";

const Example = Handler().get(async (req: NextApiRequest, res: NextApiResponse) => {

})

export default Example;